<div id="interface-form-<%= type %>" class="interface-form" data-close-on="dialog-open">
  <%= form_tag wizard_interfaces_path, :remote => true do -%>
    <table>
      <%= yield %>
    </table>
    <%= submit_tag %>
  <% end %>
</div>
